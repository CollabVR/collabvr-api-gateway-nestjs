version: '3'
services:
  backend:
    build: ./
    container_name: account_management_service
    ports:
      - '3000:3000'
    env_file:
      - .env
    networks:
      - collabvr_network

networks:
  collabvr_network:
    driver: bridge
